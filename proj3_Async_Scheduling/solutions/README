

async_behavior: test with two threads, one reads from stdin, one calculate the nth prime number. The thread that reads from stdin (I/O) must not block the thread that calculates the nth prime number

async_semantics: various tests to read the file index1.txt
